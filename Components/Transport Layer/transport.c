#include "transport.h"

char * Assemble_Packet(char *packet)
{
// TODO: write code.
    char *ch;
    return ch;
}
char * Compute_Checksum(char *packet)
{
// TODO: write code.
    char *ch;
    return ch;
}